/* Шаблон внутренних заметок 3D-модели.
   Скопируй в assets/model-private.js (он в .gitignore) и заполни. В репозиторий кладётся только шаблон.
   Блоки видны после входа admin / manager; без файла модель работает, просто без них. */
window.EDIFICE_MODEL_PRIVATE = {
  halt: { tag: "факт · дд.мм.гггг", text: "Короткая плашка поверх сцены (приостановка работ и т.п.)" },
  stages: [
    // по одному объекту на этап 00…09, в том же порядке, что EDIFICE_MODEL.stages
    { comment: "Внутренний комментарий к статусу этапа", check: ["Пункт «проверить»", "Ещё пункт"] }
  ]
};
